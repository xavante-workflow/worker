This worker will use the Xavante core library and perform operations on current workflow instances.

The idea is to simulate the whole process:
- import the workflow definition; 
- create a Process;
- Iterate the execution 
- trigger the events and update the variables
- evaluate the logs

